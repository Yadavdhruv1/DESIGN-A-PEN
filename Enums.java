enum InkType {
    BALLPOINT,
    FOUNTAIN,
    GEL
}

enum WritingColor {
    BLACK,
    BLUE,
    RED
}

enum InkLevel {
    EMPTY,
    LOW,
    FULL
}

enum PenType {
    BALLPOINT,
    FOUNTAIN,
    GEL
}

enum PenBodyMaterial {
    PLASTIC,
    METAL,
    WOOD
}